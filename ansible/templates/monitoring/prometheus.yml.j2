global:
  scrape_interval: 30s
  scrape_timeout: 10s
  evaluation_interval: 30s
  external_labels:
    cluster: 'homelab'
    environment: '{{ environment }}'

# Alerting configuration
alerting:
  alertmanagers:
    - static_configs:
        - targets: []

# Load rules once and periodically evaluate them
rule_files:
  - "{{ prometheus_config_dir }}/alert-rules.yml"

# Scrape configurations
scrape_configs:
  # Prometheus self-monitoring
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:{{ prometheus_port }}']

  # Dynamic Node Exporters - All hosts with node exporter enabled
  - job_name: 'node-exporters'
    static_configs:
{% if groups['node_exporters'] is defined %}
      - targets:
{% for host in groups['node_exporters'] %}
          - '{{ host }}.{{ domain_name }}:9100'
{% endfor %}
        labels:
          group: 'infrastructure'
{% endif %}
{% if groups['proxmox_node_exporters'] is defined %}
{% for host in groups['proxmox_node_exporters'] %}
      - targets:
          - '{{ hostvars[host].ansible_host }}:9100'
        labels:
          group: 'infrastructure'
          instance: '{{ host }}.{{ domain_name }}'
{% endfor %}
{% endif %}

  # Dynamic MySQL Exporters - All hosts with mysql exporter enabled
{% if groups.get('mysql_exporters', []) | length > 0 %}
  - job_name: 'mysql-exporters'
    static_configs:
      - targets:
{% for host in groups['mysql_exporters'] %}
          - '{{ host }}.{{ domain_name }}:9104'
{% endfor %}
        labels:
          group: 'database'
    relabel_configs:
      - source_labels: [__address__]
        target_label: instance
        replacement: '${1}'
{% endif %}

  # Optional: Proxmox API Monitoring (requires proxmox_exporter)
  # - job_name: 'proxmox-api'
  #   static_configs:
  #     - targets:
  #         - 'localhost:8001'
  #       labels:
  #         cluster: 'pve'
